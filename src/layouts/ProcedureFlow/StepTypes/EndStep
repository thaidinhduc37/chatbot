import classNames from 'classnames/bind';
import styles from '../ProcedureFlow.module.scss';

const cx = classNames.bind(styles);

function EndStep({ step }) {
    return (
        <div className={cx('procedure-end')}>
            <p>{step.question}</p>
        </div>
    );
}
export default EndStep;
